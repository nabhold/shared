# Copy into <repo>/.github/workflows/enforce-action-pinning.yml
# No adjustments usually needed — delete this comment block once copied.

name: Enforce Action Pinning

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/**"
      - ".github/actions/**"
  pull_request:
    paths:
      - ".github/workflows/**"
      - ".github/actions/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    permissions:
      contents: read
    # SHA is nabhold/shared's v1.0.0 tag (annotated — peeled commit, not
    # the tag object's own SHA), verified live via
    # `git ls-remote --tags https://github.com/nabhold/shared.git v1.0.0`
    # on 2026-08-25.
    uses: nabhold/shared/.github/workflows/enforce-action-pinning.yml@ad5cdb028023cd6b9c48cd8d7e6151ef4ab0cd73 # v1.0.0