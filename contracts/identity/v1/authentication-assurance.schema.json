{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.nabhold.com/contracts/identity/v1/authentication-assurance.schema.json",
  "title": "AuthenticationAssurance",
  "description": "Descriptive information about how a principal authenticated for the current context -- not a business authorization grant (ADR-0015 §180). Consumers use this to decide whether a risk-sensitive operation's AssuranceRequirement (assurance-requirement.schema.json) is met, not to authorize the operation itself.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "actor_type": {
      "$ref": "actor-type.schema.json"
    },
    "acr": {
      "type": "string",
      "minLength": 1,
      "description": "OIDC Authentication Context Class Reference for this authentication event, as asserted by the issuer."
    },
    "amr": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "OIDC Authentication Methods References used to establish this authentication (e.g. pwd, webauthn, otp)."
    },
    "authenticated_at": {
      "type": "string",
      "format": "date-time",
      "description": "When the underlying authentication event occurred (OIDC auth_time)."
    },
    "step_up_at": {
      "type": "string",
      "format": "date-time",
      "description": "When a step-up re-authentication most recently occurred, if any (ADR-0015 §37-39)."
    },
    "issuer": {
      "type": "string",
      "format": "uri",
      "description": "The verified OIDC issuer that authenticated this principal."
    },
    "client_id": {
      "type": "string",
      "minLength": 1,
      "description": "The OAuth client (azp) the authentication was performed for."
    }
  },
  "required": ["actor_type", "acr", "amr", "authenticated_at", "issuer", "client_id"]
}
