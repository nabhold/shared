{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.nabhold.com/contracts/identity-events/v1/identity-created.schema.json",
  "title": "IdentityCreatedEvent",
  "description": "Emitted when a new canonical identity is created. This is the event envelope's `data` payload — see asyncapi.yaml for how it composes with contracts/events/v1/envelope.schema.json.",
  "type": "object",
  "additionalProperties": false,
  "required": ["principal_id", "actor_type"],
  "properties": {
    "principal_id": {
      "type": "string",
      "format": "uuid"
    },
    "actor_type": {
      "$ref": "../../identity/v1/actor-type.schema.json"
    },
    "external_issuer": {
      "type": "string",
      "description": "The issuer that triggered creation (if any)."
    },
    "external_subject": {
      "type": "string",
      "description": "The subject that triggered creation."
    }
  }
}
