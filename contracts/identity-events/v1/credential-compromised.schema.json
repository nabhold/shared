{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.nabhold.com/contracts/identity-events/v1/credential-compromised.schema.json",
  "title": "CredentialCompromisedEvent",
  "description": "Emitted when a specific credential is identified as compromised and revoked (ADR-0016 §12-13 -- credential lifecycle is independent of Canonical Identity state; revoking one credential SHOULD invalidate only that credential unless broader compromise is suspected. ADR-0015 §77-80/§187 'Credential Compromise Flow': identify the affected credential, then revoke it). credential_type enumerates ADR-0015 §5's supported human authenticator categories. This is the event envelope's `data` payload — see asyncapi.yaml for how it composes with contracts/events/v1/envelope.schema.json.",
  "type": "object",
  "additionalProperties": false,
  "required": ["credential_id", "principal_id", "credential_type", "previous_status", "new_status", "reason", "actor"],
  "properties": {
    "credential_id": {
      "type": "string",
      "description": "Opaque IAM-assigned identifier for the specific credential (not the credential value itself)."
    },
    "principal_id": {
      "type": "string",
      "format": "uuid"
    },
    "credential_type": {
      "type": "string",
      "enum": ["password", "passkey", "webauthn_security_key", "webauthn_platform", "totp", "recovery_code", "federated_enterprise_idp", "social_idp"],
      "description": "ADR-0015 §5's supported human authenticator categories."
    },
    "previous_status": {
      "type": "string",
      "enum": ["ACTIVE"]
    },
    "new_status": {
      "type": "string",
      "enum": ["COMPROMISED"]
    },
    "reason": {
      "type": "string",
      "description": "Machine-readable reason code."
    },
    "actor": {
      "type": "string",
      "description": "Who performed the action (system or human ID)."
    }
  }
}
