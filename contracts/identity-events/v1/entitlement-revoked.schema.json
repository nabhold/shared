{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.nabhold.com/contracts/identity-events/v1/entitlement-revoked.schema.json",
  "title": "EntitlementRevokedEvent",
  "description": "Emitted when a capability entitlement is revoked or expires (ADR-0016 §23-24 -- capability entitlement has an independent lifecycle from identity and membership; revoking one capability, e.g. ERP, does not revoke unrelated ones, e.g. Trade/CMS/Pulse, unless policy explicitly couples them). This is the event envelope's `data` payload — see asyncapi.yaml for how it composes with contracts/events/v1/envelope.schema.json.",
  "type": "object",
  "additionalProperties": false,
  "required": ["entitlement_id", "principal_id", "capability_key", "previous_status", "new_status", "reason", "actor"],
  "properties": {
    "entitlement_id": {
      "type": "string",
      "format": "uuid"
    },
    "principal_id": {
      "type": "string",
      "format": "uuid"
    },
    "capability_key": {
      "type": "string",
      "description": "The implementation-neutral capability this entitlement grants (e.g. baobab_erp, baobab_trade)."
    },
    "previous_status": {
      "type": "string",
      "enum": ["ACTIVE", "SUSPENDED"]
    },
    "new_status": {
      "type": "string",
      "enum": ["REVOKED", "EXPIRED"]
    },
    "reason": {
      "type": "string",
      "description": "Machine-readable reason code."
    },
    "actor": {
      "type": "string",
      "description": "Who performed the action (system or human ID)."
    }
  }
}
